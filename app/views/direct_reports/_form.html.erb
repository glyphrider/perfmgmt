<%= form_for(direct_report) do |f| %>
  <% if direct_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(direct_report.errors.count, "error") %> prohibited this direct_report from being saved:</h2>

      <ul>
      <% direct_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-default" %>
  </div>
<% end %>
