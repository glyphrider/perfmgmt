<div class="container">
<p class="alert alert-info" id="notice"><%= notice %></p>

<h1>Meeting with <%= @direct_report.name %></h1>
<p>at <%= @meeting.time.strftime("%m/%d %I:%M%p") %></p>

<p>
  <strong>Notes:</strong>
  <%= @meeting.notes %>
</p>

<%= link_to 'Edit', edit_direct_report_meeting_path(@meeting.direct_report_id,@meeting.id) %> |
<%= link_to 'Back', direct_reports_path %>
</div>
